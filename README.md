# 🔐 Smart SSH with Tailscale (Zsh)

เชื่อมต่อ SSH แบบอัตโนมัติ โดยเลือกเส้นทางที่ดีที่สุด

> ถ้า **Tailscale VPN เปิดอยู่** → ใช้ **Tailscale IP**  
> ถ้า **ไม่เปิด / ไม่พร้อม** → ใช้ **LAN IP**

พิมพ์แค่:
```bash
your server name
